# Test generic type aliases

# Define a concrete type
Hash32 = Vector[byte, 32]

# Generic class
class Box[T](Container):
    value: T
    count: uint64

# Create type alias for specific instantiation
BoxHash32 = Box[Hash32]

# Use the alias in another class
class Warehouse(Container):
    item: BoxHash32
    id: uint64

