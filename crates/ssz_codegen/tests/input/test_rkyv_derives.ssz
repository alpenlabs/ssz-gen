Slot = uint64

### Test that rkyv derives use fully qualified paths (mirrors identifiers use case)
#~# derive: std::marker::Copy, std::hash::Hash, rkyv::Archive, rkyv::Serialize, rkyv::Deserialize
class BlockCommitment(Container):
    ### Slot number
    slot: Slot
    ### Block ID
    blkid: uint256

class OtherType(Container):
    value: uint64
